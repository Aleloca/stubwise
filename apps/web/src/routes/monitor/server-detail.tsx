/**
 * Placeholder del dettaglio server (`/monitor/servers/$serverId`): esiste solo
 * perché la route sia registrata e il `Link` tipato della {@link ServerCard}
 * compili e navighi. Il contenuto reale (4 pannelli uPlot, servizi, check,
 * soglie) arriva in E3, che sostituisce questo componente.
 */
export function ServerDetailPage() {
  return null;
}
