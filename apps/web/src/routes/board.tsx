import { PagePlaceholder } from "../components/page-placeholder";

export function BoardPage() {
  return (
    <PagePlaceholder
      title="Board"
      description="Kanban del ciclo di vita: open → triaged → in progress → in review → done. In arrivo nel Task 17."
    />
  );
}
