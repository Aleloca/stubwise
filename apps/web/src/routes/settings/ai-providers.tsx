import { AiProvidersSection } from "../../components/ai-providers-section";

/**
 * Sotto-pagina AI providers (solo admin): catena di credenziali della pipeline
 * AI. La logica vive in AiProvidersSection.
 */
export function SettingsAiProvidersPage() {
  return <AiProvidersSection />;
}
