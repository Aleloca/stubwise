import { PluginsSection } from "../../components/plugins-section";

/**
 * Sotto-pagina Plugin (solo admin): registro d'istanza dei plugin di Claude
 * Code. La logica vive in PluginsSection.
 */
export function SettingsPluginsPage() {
  return <PluginsSection />;
}
