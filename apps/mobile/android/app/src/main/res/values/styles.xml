<resources>

    <!-- Base application theme. Material3 (non AppCompat) è richiesto da
         react-native-bottom-tabs (Task 6, App M1+M2, 11 set 2026): la tab
         bar nativa Android usa componenti Material3 che si aspettano un
         tema Material3 nell'albero, non semplicemente compatibile con
         AppCompat. Verificato prima del cambio che `AppTheme` non è usato
         altrove nell'app (un solo riferimento, in AndroidManifest.xml, il
         tema di default dell'app) — nessun'altra activity o stile ne
         dipende. -->
    <style name="AppTheme" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
    </style>

</resources>
