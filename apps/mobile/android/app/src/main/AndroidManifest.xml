<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Task 19 (push): su Android 13+ (API 33) le notifiche sono NEGATE di
         default finché l'app non chiede — e l'utente non concede — questo
         permesso a runtime (`notifee.requestPermission()`, già chiamato da
         `OnboardingScreen`). La dichiarazione qui è il prerequisito perché
         quella richiesta sia possibile: senza, il SO la rifiuta da sé, senza
         nemmeno mostrare il dialogo. Il Firebase Messaging SDK (dipendenza
         transitiva di `@react-native-firebase/messaging`, non visibile in
         questo node_modules JS) la dichiara probabilmente già da sé nel
         proprio AAR — esplicita qui comunque: innocua se duplicata (il
         merger dei manifest Android deduplica), e rende il requisito
         leggibile da chi guarda SOLO il manifest dell'app. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        <!-- Deep link `stubwise://…` (Task 13): android:launchMode
             "singleTask" sopra fa sì che un secondo link riusi l'activity
             già aperta (onNewIntent) invece di impilarne una seconda. -->
        <intent-filter android:autoVerify="false">
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.DEFAULT" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="stubwise" />
        </intent-filter>
      </activity>
    </application>
</manifest>
