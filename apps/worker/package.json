{
  "name": "@stubwise/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "pnpm --filter @stubwise/db... build && vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@stubwise/db": "workspace:*",
    "drizzle-orm": "^0.45.2"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "vitest": "^3.2.0"
  }
}
