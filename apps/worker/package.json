{
  "name": "@stubwise/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "pnpm --filter @stubwise/db... --filter @stubwise/git... build && vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@stubwise/db": "workspace:*",
    "@stubwise/git": "workspace:*",
    "@stubwise/shared": "workspace:*",
    "drizzle-orm": "^0.45.2",
    "execa": "^9.5.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "vitest": "^3.2.0"
  }
}
