import { describe, expect, it } from "vitest";

// Smoke test dello scaffold. I test veri arrivano con la logica del server MCP.
describe("@stubwise/mcp scaffold", () => {
  it("builds and loads", () => {
    expect(true).toBe(true);
  });
});
